# Hardware-Accelerated-Network-Intrusion-Detection-System
A layer 2 based IDS that analyzes network traffic captured from a PCAP dataset, extracts relevant traffic features, and uses Principal Component Analysis (PCA) to identify anomalous or malicious behavior. The detection logic is then translated into an FPGA-friendly hardware architecture that can inspect packet streams in real time.
